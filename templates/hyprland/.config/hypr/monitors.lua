-- Monitor rules saved by keystone-monitor-menu live here.
-- Declare the anchor first; place dependent displays after it.
hl.monitor({ output = "", mode = "preferred", position = "auto", scale = 1 })
